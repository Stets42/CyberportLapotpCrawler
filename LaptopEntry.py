class LaptopEntry:
    def __init__(self, name, url, processor, ram, drive, battery):
        self.name = name
        self.url = url
        self.processor = processor
        self.ram = ram
        self.drive = drive
        self.battery = battery
